# Procedural Scene Artifacts

This folder is for generated `.jvscene` files and is intentionally not tracked in git.

Generate the procedural set on demand:

```bash
just scenes-generate-procedural
```

The generator source of truth is:

- `tools/generate_procedural_scene_files.py`
